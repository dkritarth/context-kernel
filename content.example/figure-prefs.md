# Figure / plot preferences

- Tooling: matplotlib for quick plots, D3 for anything interactive/published.
- Style: minimal gridlines, no chart borders, muted color palette (avoid default matplotlib
  colors).
- Always label axes with units. Never use a legend when direct labeling is possible.
- Export as SVG when the target is a document; PNG at 2x for slides.
